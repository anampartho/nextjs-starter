# Check build

echo '🧪 Linting your app...'
pnpm lint ||
(
  echo '🚨 🚨 🚨 next lint failed!';
  exit 1;
)

echo '🧪 Checking code formatting...'
pnpm format:check ||
(
  echo '🚨 🚨 🚨 format:check failed!';
  exit 1;
)

echo '🔨 Checking next build...'
pnpm build ||
(
  echo '🚨 🚨 🚨 next build failed!';
  exit 1;
)
